<Window x:Class="RincoNhan.Tools.ElementsTags.UI.ElementsTagsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RincoNhan.Tools.ElementsTags.UI"
        Title="Elements Tags" SizeToContent="Width" Height="650" MinHeight="450" MinWidth="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" 
        Background="#FFFFFF"
        Foreground="#1F2937" 
        FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <local:RevitColorToWpfBrushConverter x:Key="RevitColorToWpfBrushConverter"/>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <Style TargetType="Button">
                <Setter Property="Background" Value="#2563EB"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="20,10"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.9"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="SecondaryButton" TargetType="Button">
                <Setter Property="Background" Value="#F3F4F6"/>
                <Setter Property="Foreground" Value="#1F2937"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="#D1D5DB"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="450" /> <!-- Main UI -->
            <ColumnDefinition Width="Auto" /> <!-- Errors -->
        </Grid.ColumnDefinitions>

        <TabControl Grid.Column="0" Background="Transparent" BorderThickness="0" Margin="0,-5,0,0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Padding="15,8" Margin="0,0,5,10" Background="Transparent" BorderThickness="0,0,0,2" BorderBrush="Transparent">
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="0,2"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#2563EB"/>
                                        <Setter Property="Foreground" Value="#2563EB"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            
            <TabItem Header="PLAN CHECK">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Status -->
                        <RowDefinition Height="*"/>    <!-- Category List -->
                        <RowDefinition Height="Auto"/> <!-- Options -->
                        <RowDefinition Height="Auto"/> <!-- Actions -->
                    </Grid.RowDefinitions>

                    <!-- Status Notification Bar -->
                    <Border Grid.Row="0" Background="#F3F4F6" CornerRadius="6" Margin="0,0,0,10" Padding="15,10" Visibility="{Binding StatusVisible, Converter={StaticResource BoolToVis}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ℹ " Foreground="#2563EB" FontWeight="Bold" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding StatusMessage}" Foreground="#1F2937" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <!-- Category List Grid -->
                    <Border Grid.Row="1" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Background="#F9FAFB">
                        <DataGrid ItemsSource="{Binding Categories}" 
                                  AutoGenerateColumns="False" 
                                  CanUserAddRows="False"
                                  HeadersVisibility="Column"
                                  Background="Transparent"
                                  RowBackground="Transparent"
                                  BorderThickness="0"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#E5E7EB">
                            <DataGrid.Columns>
                                <DataGridCheckBoxColumn Header="✓" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="40">
                                    <DataGridCheckBoxColumn.ElementStyle>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridCheckBoxColumn.ElementStyle>
                                </DataGridCheckBoxColumn>
                                
                                <DataGridTextColumn Header="CATEGORY" Binding="{Binding Name}" IsReadOnly="True" Width="*"/>
                                
                                <DataGridTemplateColumn Header="COLOR" Width="60">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Command="{Binding DataContext.PickColorCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Padding="4"
                                                    HorizontalAlignment="Center"
                                                    ToolTip="Pick Override Color">
                                                <Border Width="16" Height="16" 
                                                        BorderThickness="4" 
                                                        CornerRadius="2"
                                                        BorderBrush="{Binding OverrideColor, Converter={StaticResource RevitColorToWpfBrushConverter}, UpdateSourceTrigger=PropertyChanged}"
                                                        Background="Transparent" />
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="TAG TYPE" Width="1.5*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding TagTypes}" 
                                                      SelectedItem="{Binding SelectedTagType, UpdateSourceTrigger=PropertyChanged}"
                                                      DisplayMemberPath="Name" 
                                                      Margin="2"
                                                      Height="28"
                                                      VerticalContentAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- Options -->
                    <StackPanel Grid.Row="2" Margin="0,15,0,15" Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding AddLeader}" Content="Add Leader" Margin="0,0,20,0"/>
                        <CheckBox IsChecked="{Binding OnlyUntagged}" Content="Only tag untagged elements" IsEnabled="False" Opacity="0.5"/>
                    </StackPanel>

                    <!-- Actions -->
                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <Button Command="{Binding TagAllCommand}" Content="TAG ALL SELECTED" Height="45" Margin="0,0,0,8" Background="#2563EB"/>
                            
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.5*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Button Command="{Binding CheckTag2DCommand}" Content="Check tag host 2d" Style="{StaticResource SecondaryButton}" Height="38" Grid.Column="0" Margin="0,0,5,0" FontWeight="Bold"/>
                                <Button Command="{Binding ClashTagCommand}" Content="Clash tag" Style="{StaticResource SecondaryButton}" Height="38" Grid.Column="1" FontWeight="Bold"/>
                            </Grid>

                            <Button Command="{Binding CheckFloorCommand}" Content="Check 3d" Style="{StaticResource SecondaryButton}" Height="38" Margin="0,0,0,0"/>
                        </StackPanel>
                        
                        <Button Command="{Binding ResetCommand}" Content="RESET" Style="{StaticResource SecondaryButton}" Grid.Column="1" Margin="8,0,0,0" Width="70" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="SECTION CHECK">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Border Grid.Row="0" Background="#F3F4F6" CornerRadius="6" Margin="0,0,0,10" Padding="15,10" Visibility="{Binding StatusVisible, Converter={StaticResource BoolToVis}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ℹ " Foreground="#2563EB" FontWeight="Bold" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding StatusMessage}" Foreground="#1F2937" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <CheckBox Grid.Row="1" IsChecked="{Binding AutoRehostSection}" Content="Auto Pick New Host (Tự động Re-host)" Margin="0,0,0,10" FontWeight="SemiBold"/>

                    <Grid Grid.Row="2" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Command="{Binding CheckWallTagSectionCommand}" Content="CHECK HOST CỦA WALL TAG" Height="45" Background="#2563EB"/>
                        <Button Grid.Column="2" Command="{Binding UpdateSelectedTagsCommand}" Content="UPDATE SELECTED" Height="45" Padding="20,0" Background="#10B981"/>
                        <Button Grid.Column="4" Command="{Binding ResetCommand}" Content="RESET MÀU" Height="45" Padding="20,0" Background="#EF4444" Foreground="White" BorderThickness="0"/>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Errors -->
        <Grid Grid.Column="1" Margin="15,0,0,0" Visibility="{Binding HasErrors, Converter={StaticResource BoolToVis}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="RESULT" FontWeight="Bold" FontSize="13" Foreground="#1F2937" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Command="{Binding UncheckAllCommand}" Content="UNCHECK ALL" Style="{StaticResource SecondaryButton}" Padding="15,5" Height="30"/>
            </Grid>
            
            <Border Grid.Row="1" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Background="#F9FAFB" VerticalAlignment="Stretch">
                <DataGrid ItemsSource="{Binding ErrorResults}" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          Background="Transparent"
                          RowBackground="Transparent"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#E5E7EB"
                          VerticalAlignment="Stretch"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="30"/>
                    <DataGridTextColumn Header="ID" Binding="{Binding IdValue}" IsReadOnly="True" Width="80"/>
                    <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category}" IsReadOnly="True" Width="100"/>
                    <DataGridTextColumn Header="TYPE" Binding="{Binding ErrorType}" IsReadOnly="True" Width="100"/>
                    <DataGridTemplateColumn Header="ACTION" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Show" 
                                        Command="{Binding DataContext.ShowElementCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                        CommandParameter="{Binding}" 
                                        Style="{StaticResource SecondaryButton}" 
                                        Padding="5,2" 
                                        Margin="2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            </Border>
        </Grid>
    </Grid>
</Window>
