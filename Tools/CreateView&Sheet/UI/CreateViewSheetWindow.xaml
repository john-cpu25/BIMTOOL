<Window x:Class="RincoNhan.Tools.CreateViewSheet.UI.CreateViewSheetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:models="clr-namespace:RincoNhan.Tools.CreateViewSheet.Models"
        xmlns:ui="clr-namespace:RincoNhan.Tools.CreateViewSheet.UI"
        mc:Ignorable="d"
        Title="Create View &amp; Sheet Tool" Height="680" Width="950"
        WindowStartupLocation="CenterScreen"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResizeWithGrip"
        Background="White">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <ui:StringMatchConverter x:Key="StringMatchConverter"/>
        
        <ObjectDataProvider x:Key="DuplicateModeEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:DuplicateMode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FDFDFD"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Padding" Value="14"/>
        </Style>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2E5C9A"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="#2E5C9A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E3C6A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DCDDE1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F0F0F0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="False"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="RowHeight" Value="30"/>
        </Style>

        <Style x:Key="ColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F5F7F9"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <CollectionViewSource x:Key="GroupedSheets" Source="{Binding ProjectSheets}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="SheetSeries"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </Window.Resources>

    <Grid Background="#F5F7F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header removed -->

        <!-- Main Content (Tabs) -->
        <TabControl Grid.Row="1" Margin="10" Background="Transparent" BorderThickness="0">
            <!-- TAB 1: CREATE VIEW -->
            <TabItem Header="Create View">
                <Grid Background="White" Margin="0,10,0,0">
                    <Grid.Resources>
                        <CollectionViewSource x:Key="GroupedCreateViewRows" Source="{Binding CreateViewRows}" IsLiveGroupingRequested="True">
                            <CollectionViewSource.LiveGroupingProperties>
                                <sys:String>SelectedLevel.DisplayName</sys:String>
                            </CollectionViewSource.LiveGroupingProperties>
                            <CollectionViewSource.GroupDescriptions>
                                <PropertyGroupDescription PropertyName="SelectedLevel.DisplayName" />
                            </CollectionViewSource.GroupDescriptions>
                        </CollectionViewSource>
                    </Grid.Resources>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="10">
                        <Button Content="+ Add Row" Command="{Binding AddCreateViewRowCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="30" Margin="0,0,10,0"/>
                        <Button Content="Add All Levels" Command="{Binding AddAllLevelsToCreateViewCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="30" Background="#27AE60" Margin="0,0,10,0"/>
                        <TextBlock Text="Select level, suffix, view type, template, and duplicate mode." VerticalAlignment="Center" Foreground="#7F8C8D"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Source={StaticResource GroupedCreateViewRows}}" Style="{StaticResource DataGridStyle}" ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" Margin="10">
                        <DataGrid.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Margin" Value="0,0,0,5"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander IsExpanded="True" Background="#F4F6F9" BorderBrush="#DCDDE1" BorderThickness="1">
                                                        <Expander.Header>
                                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#2C3E50" Margin="5,0"/>
                                                        </Expander.Header>
                                                        <ItemsPresenter />
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </DataGrid.GroupStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="✓" Width="40">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_ShiftClick" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTemplateColumn Header="Level" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.ProjectLevels, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                  SelectedItem="{Binding SelectedLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                  DisplayMemberPath="DisplayName" Margin="2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Suffix" Binding="{Binding Suffix, UpdateSourceTrigger=PropertyChanged}" Width="150"/>

                            <DataGridTemplateColumn Header="View Type" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.ViewTypes, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                  SelectedItem="{Binding SelectedViewType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                  DisplayMemberPath="Name" Margin="2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="View Template" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.ViewTemplates, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                  SelectedItem="{Binding SelectedViewTemplate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                  DisplayMemberPath="Name" Margin="2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Duplicate Mode" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.DuplicateModes, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                  SelectedItem="{Binding DuplicateMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Actions" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Remove" 
                                                Command="{Binding DataContext.RemoveCreateViewRowCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                CommandParameter="{Binding}"
                                                Foreground="Red" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Button Grid.Row="2" Content="Create Views" Command="{Binding GenerateViewsCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Margin="10" Width="150"/>
                </Grid>
            </TabItem>

            <!-- TAB 2: CREATE SHEET -->
            <TabItem Header="Create Sheet">
                <Grid Background="White" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Margin="10">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Titleblock:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                            <ComboBox ItemsSource="{Binding TitleBlocks}" SelectedItem="{Binding SelectedTitleBlock}" DisplayMemberPath="Name" Width="300" Height="28"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Sheet Series:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                            <ComboBox ItemsSource="{Binding SheetSeriesOptions}" SelectedItem="{Binding SelectedSheetSeries}" Width="400" Height="28" IsEditable="True"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal">
                            <Button Content="+ Add Row" Command="{Binding AddCreateSheetRowCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="30" Margin="0,0,10,0"/>
                            <Button Content="Add All Levels" Command="{Binding AddAllLevelsToCreateSheetCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="30" Background="#27AE60" Margin="0,0,10,0"/>
                            <TextBlock Text="Add rows to generate sheets from levels." VerticalAlignment="Center" Foreground="#7F8C8D"/>
                        </StackPanel>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding CreateSheetRows}" Style="{StaticResource DataGridStyle}" ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" Margin="10">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="✓" Width="40">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_ShiftClick" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTextColumn Header="Sheet Number" Binding="{Binding SheetNumber, UpdateSourceTrigger=PropertyChanged}" Width="120"/>

                            <DataGridTemplateColumn Header="Level" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.ProjectLevels, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                  SelectedItem="{Binding SelectedLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                  DisplayMemberPath="DisplayName" Margin="2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Suffix" Binding="{Binding Suffix, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                            <DataGridTextColumn Header="Sheet Name" Binding="{Binding SheetName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>

                            <DataGridTemplateColumn Header="Actions" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Remove" 
                                                Command="{Binding DataContext.RemoveCreateSheetRowCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                CommandParameter="{Binding}"
                                                Foreground="Red" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Button Grid.Row="2" Content="Create Sheets" Command="{Binding GenerateSheetsCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Margin="10" Width="150"/>
                </Grid>
            </TabItem>

            <!-- TAB 3: ADD VIEWS TO SHEET -->
            <TabItem Header="Add Views to Sheet">
                <Grid Background="White" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="10,10,10,0">
                        <TextBlock Text="Select Views" Style="{StaticResource SectionLabel}" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding ViewSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="28" Padding="6,4" VerticalContentAlignment="Center"
                                     BorderBrush="#DCDDE1" BorderThickness="1"/>
                            <Button Grid.Column="1" Content="All" Command="{Binding SelectAllViewsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Height="28" Width="50" Margin="5,0,0,0" FontSize="11" Padding="0"/>
                            <Button Grid.Column="2" Content="None" Command="{Binding DeselectAllViewsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Height="28" Width="50" Margin="5,0,0,0" FontSize="11" Padding="0" Background="#95A5A6"/>
                        </Grid>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="10,10,10,0">
                        <TextBlock Text="Select Target Sheets" Style="{StaticResource SectionLabel}" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding SheetSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="28" Padding="6,4" VerticalContentAlignment="Center"
                                     BorderBrush="#DCDDE1" BorderThickness="1"/>
                            <Button Grid.Column="1" Content="All" Command="{Binding SelectAllSheetsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Height="28" Width="50" Margin="5,0,0,0" FontSize="11" Padding="0"/>
                            <Button Grid.Column="2" Content="None" Command="{Binding DeselectAllSheetsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Height="28" Width="50" Margin="5,0,0,0" FontSize="11" Padding="0" Background="#95A5A6"/>
                        </Grid>
                    </StackPanel>

                    <DataGrid Grid.Column="0" Grid.Row="1" ItemsSource="{Binding FilteredViews}" Style="{StaticResource DataGridStyle}" ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" Margin="10">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="✓" Width="40">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_ShiftClick" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="View Name" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <DataGrid Grid.Column="1" Grid.Row="1" ItemsSource="{Binding FilteredSheets}" Style="{StaticResource DataGridStyle}" ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" Margin="10">
                        <DataGrid.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander IsExpanded="True">
                                                        <Expander.Header>
                                                            <Border Background="#2E5C9A" Padding="8,4" CornerRadius="3" MinWidth="300">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                                                    <TextBlock Text=" (" Foreground="#B0C4DE" FontSize="11"/>
                                                                    <TextBlock Text="{Binding ItemCount}" Foreground="#B0C4DE" FontSize="11"/>
                                                                    <TextBlock Text=" sheets)" Foreground="#B0C4DE" FontSize="11"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </Expander.Header>
                                                        <ItemsPresenter/>
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </DataGrid.GroupStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="✓" Width="40">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_ShiftClick" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Number" Binding="{Binding SheetNumber}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding SheetName}" Width="*" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource CardStyle}" Margin="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="Stack Views" IsChecked="{Binding StackViews}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Text="(Places all selected views on the first selected sheet)" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#7F8C8D" FontStyle="Italic"/>
                                </StackPanel>
                                <TextBlock Text="Thứ tự đặt view:  ① ARCH  →  ② UNDER  →  ③ OVER  →  ④ Others" 
                                           Margin="0,4,0,0" Foreground="#2E5C9A" FontSize="11" FontWeight="SemiBold"/>
                            </StackPanel>
                            
                            <Button Grid.Column="1" Content="Add Views to Sheet" Command="{Binding AddViewsToSheetsCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="160"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- TAB 4: ADD VIEWS MULTI -->
            <TabItem Header="Add Views Multi">
                <Grid Background="White" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Toolbar -->
                    <StackPanel Grid.Row="0" Margin="10,10,10,0">
                        <TextBlock Text="Auto-match views to sheets by level" Style="{StaticResource SectionLabel}" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding MultiSheetSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="28" Padding="6,4" VerticalContentAlignment="Center"
                                     BorderBrush="#DCDDE1" BorderThickness="1"/>
                            <Button Grid.Column="1" Content="Auto Match" Command="{Binding AutoMatchViewsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Height="28" Margin="5,0,0,0" FontSize="11" Padding="12,0" Background="#27AE60"/>
                            <Button Grid.Column="2" Content="All" Command="{Binding SelectAllMultiRowsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Height="28" Width="50" Margin="5,0,0,0" FontSize="11" Padding="0"/>
                            <Button Grid.Column="3" Content="None" Command="{Binding DeselectAllMultiRowsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Height="28" Width="50" Margin="5,0,0,0" FontSize="11" Padding="0" Background="#95A5A6"/>
                        </Grid>
                    </StackPanel>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredMultiRows}" Style="{StaticResource DataGridStyle}" ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" Margin="10">
                        <DataGrid.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander IsExpanded="True">
                                                        <Expander.Header>
                                                            <Border Background="#2E5C9A" Padding="8,4" CornerRadius="3" MinWidth="800">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                                                    <TextBlock Text=" (" Foreground="#B0C4DE" FontSize="11"/>
                                                                    <TextBlock Text="{Binding ItemCount}" Foreground="#B0C4DE" FontSize="11"/>
                                                                    <TextBlock Text=" sheets)" Foreground="#B0C4DE" FontSize="11"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </Expander.Header>
                                                        <ItemsPresenter/>
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </DataGrid.GroupStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="✓" Width="35">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_ShiftClick" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Sheet #" Binding="{Binding Sheet.SheetNumber}" Width="80" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Sheet Name" Binding="{Binding Sheet.SheetName}" Width="180" IsReadOnly="True"/>

                            <DataGridTemplateColumn Header="① ARCH View" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.ViewOptionsForMulti, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                  SelectedItem="{Binding ArchView, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                  DisplayMemberPath="Name" Margin="2" Height="24"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="② UNDER View" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.ViewOptionsForMulti, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                  SelectedItem="{Binding UnderView, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                  DisplayMemberPath="Name" Margin="2" Height="24"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="③ OVER View" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.ViewOptionsForMulti, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                  SelectedItem="{Binding OverView, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                  DisplayMemberPath="Name" Margin="2" Height="24"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Bottom bar -->
                    <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Thứ tự đặt view:  ① ARCH  →  ② UNDER  →  ③ OVER" 
                                       VerticalAlignment="Center" Foreground="#2E5C9A" FontSize="11" FontWeight="SemiBold"/>

                            <Button Grid.Column="1" Content="Add All to Sheets" Command="{Binding AddMultiViewsToSheetsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Width="160"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- TAB 5: VIEW TITLE -->
            <TabItem Header="View Title">
                <Grid Background="White" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Toolbar -->
                    <StackPanel Grid.Row="0" Margin="10,10,10,0">
                        <TextBlock Text="Change Viewport Title Type for views on sheets" Style="{StaticResource SectionLabel}" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding TitleSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="28" Padding="6,4" VerticalContentAlignment="Center"
                                     BorderBrush="#DCDDE1" BorderThickness="1"/>
                            <Button Grid.Column="1" Content="All" Command="{Binding SelectAllTitleRowsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Height="28" Width="50" Margin="5,0,0,0" FontSize="11" Padding="0"/>
                            <Button Grid.Column="2" Content="None" Command="{Binding DeselectAllTitleRowsCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Height="28" Width="50" Margin="5,0,0,0" FontSize="11" Padding="0" Background="#95A5A6"/>
                        </Grid>
                    </StackPanel>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredTitleRows}" Style="{StaticResource DataGridStyle}" ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" Margin="10">
                        <DataGrid.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander IsExpanded="True">
                                                        <Expander.Header>
                                                            <Border Background="#8E44AD" Padding="8,4" CornerRadius="3" MinWidth="800">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                                                    <TextBlock Text=" (" Foreground="#D2B4DE" FontSize="11"/>
                                                                    <TextBlock Text="{Binding ItemCount}" Foreground="#D2B4DE" FontSize="11"/>
                                                                    <TextBlock Text=" views)" Foreground="#D2B4DE" FontSize="11"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </Expander.Header>
                                                        <ItemsPresenter/>
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </DataGrid.GroupStyle>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsOnSheet}" Value="False">
                                        <Setter Property="Opacity" Value="0.4"/>
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="✓" Width="35">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_ShiftClick" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="View Name" Binding="{Binding ViewName}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Sheet #" Binding="{Binding SheetNumber}" Width="70" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Current Title" Binding="{Binding CurrentTitle}" Width="160" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Bottom bar -->
                    <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Apply Title:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding ViewportTitleTypes}" SelectedItem="{Binding SelectedViewportTitle}" 
                                      DisplayMemberPath="Name" Height="28" Margin="0,0,15,0"/>
                            <Button Grid.Column="2" Content="Apply to Selected" Command="{Binding ApplyViewportTitleCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}" Background="#8E44AD" Width="160"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- TAB 6: ALIGN VIEWS -->
            <TabItem Header="Align Views">
                <Grid Background="White" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Select Target Sheets" Style="{StaticResource SectionLabel}" Margin="10,10,0,5"/>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Source={StaticResource GroupedSheets}}" Style="{StaticResource DataGridStyle}" ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}" Margin="10">
                        <DataGrid.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander IsExpanded="True">
                                                        <Expander.Header>
                                                            <Border Background="#2E5C9A" Padding="8,4" CornerRadius="3" MinWidth="300">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                                                    <TextBlock Text=" (" Foreground="#B0C4DE" FontSize="11"/>
                                                                    <TextBlock Text="{Binding ItemCount}" Foreground="#B0C4DE" FontSize="11"/>
                                                                    <TextBlock Text=" sheets)" Foreground="#B0C4DE" FontSize="11"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </Expander.Header>
                                                        <ItemsPresenter/>
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </DataGrid.GroupStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="✓" Width="40">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Click="CheckBox_ShiftClick" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Number" Binding="{Binding SheetNumber}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding SheetName}" Width="*" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="10">
                        <StackPanel>
                            <TextBlock Text="Align Views Across Sheets" Style="{StaticResource SectionLabel}"/>
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Text="Template View:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                                <ComboBox Grid.Column="1" ItemsSource="{Binding ProjectViews}" SelectedItem="{Binding TemplateViewForAlign}" DisplayMemberPath="Name" Height="28"/>
                            </Grid>
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="Align:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                                <RadioButton Grid.Column="1" Content="View" GroupName="AlignMode" VerticalAlignment="Center" Margin="0,0,15,0"
                                             IsChecked="{Binding AlignMode, Converter={StaticResource StringMatchConverter}, ConverterParameter=View}"/>
                                <RadioButton Grid.Column="2" Content="Title" GroupName="AlignMode" VerticalAlignment="Center" Margin="0,0,15,0"
                                             IsChecked="{Binding AlignMode, Converter={StaticResource StringMatchConverter}, ConverterParameter=Title}"/>
                                <RadioButton Grid.Column="3" Content="View + Title" GroupName="AlignMode" VerticalAlignment="Center" Margin="0,0,15,0"
                                             IsChecked="{Binding AlignMode, Converter={StaticResource StringMatchConverter}, ConverterParameter=View + Title}"/>

                                <Button Grid.Column="5" Content="Align Views on Selected Sheets" Command="{Binding AlignViewsCommand}" Style="{StaticResource PrimaryButtonStyle}" Background="#27AE60" Width="220"/>
                            </Grid>
                            <TextBlock Text="Views on selected sheets will be aligned to match the template view position. (Legends are ignored)." 
                                       Margin="0,5,0,0" FontSize="11" Foreground="#7F8C8D"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#ECF0F1" Padding="10,5">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#2C3E50" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</Window>
